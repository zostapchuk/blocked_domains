# usage: unblock.py [-h] [-r] [-c] [-l]

## options:
###  -h, --help  show this help message and exit
###  -r          read files instead of asking APIs
###  -c          use custom domains file - list.txt
###  -l          if set, log to unblock.log instead of stdout
